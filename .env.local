# Local development overrides
# Keep the main college backend on Railway from frontend/.env.
# Only point Hostel ERP requests to the locally running ERP backend.
VITE_ERP_API_BASE=http://127.0.0.1:8000
VITE_ERP_DEV_PROXY_TARGET=http://127.0.0.1:8000
