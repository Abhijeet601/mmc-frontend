// StatsCounter removed by request — 'Our Impact' section has been deleted.
// Keeping a small stub to avoid accidental import/build errors in other files.
import React from 'react';

const StatsCounter = () => null;

export default StatsCounter;
